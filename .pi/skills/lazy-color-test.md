---
name: lazy-color-test
description: Test skill for lazy tool auto-loading. Use when verifying that loading a skill automatically loads the lazy_color tool.
tools:
  - lazy_color
---

# Lazy Color Test

This skill exists to verify skill-associated lazy tool loading.

After loading this skill, call `lazy_color` to confirm it was automatically loaded.
